// Прототип функции
int sum(int a, int b);

// Функция, использующая прототип
int main() {
    int result = sum(3, 4);
    return result;
}

// Определение функции
int sum(int a, int b) {
    return a + b;
}
